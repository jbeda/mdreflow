This is a paragraph with multiple sentences. Each sentence should end up on
its own line after reflow in sentence mode, but para mode joins the whole
paragraph to a single line instead. Here is a third sentence to be sure.

A second paragraph follows after a blank line. It also joins to one line.
