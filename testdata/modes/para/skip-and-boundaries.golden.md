Intro paragraph before any directive. It has two sentences here.

:::note

A blank-line-delimited note body with two sentences. It should join to one line same as any other paragraph.

:::

> [!NOTE]
> This is an alert body with two sentences. It should join to one line around the marker line.

```go
fmt.Println("code fences pass through byte for byte")
```

Closing paragraph after everything. It also has two sentences.
