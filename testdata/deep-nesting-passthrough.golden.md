Intro paragraph before the deeply nested structure.
It has two sentences here.

> - Bullet item inside a quote, containing another quote below it.
>
>   > Doubly nested quote inside a list item inside a quote. This should pass through untouched since it is too deep.

Closing paragraph after the deeply nested structure.
It also has two sentences.
