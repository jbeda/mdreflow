Intro paragraph before the composition. It has two sentences here.

> - Bullet item inside a blockquote has multiple sentences. It composes quote and list prefixes.
> - Second bullet item inside the quote is short.

- Item containing a nested quote has multiple sentences. It composes list and quote prefixes.

  > Quoted aside nested inside a list item. It should keep the list indent plus a quote marker.

Closing paragraph after the composition. It also has two sentences.
